import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: {
    default: "AdvocateAI — Your Consumer Rights Assistant",
    template: "%s | AdvocateAI",
  },
  description:
    "AI-powered tools to navigate bureaucracy, understand your rights, and generate dispute letters. Turn complex legal jargon into plain-language action plans.",
  keywords: [
    "consumer rights",
    "dispute letter",
    "bureaucracy",
    "legal help",
    "AI assistant",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}